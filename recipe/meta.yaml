{% set version = "1.1.5" %}

package:
  name: bgenix
  version: "{{ version|replace("-", "_") }}"

source:
  url: https://github.com/huntdatacenter/rbgen/releases/download/{{ version }}/bgen-{{ version }}.tar.bz2
  sha256: af8ada5bad49cb67c47fb52203e7ad32f8c06b16cd02475f141c8920017280dd

build:
  skip: true  # [win]
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('fortran') }}
    - boost {{ boost }}
    - boost-cpp {{ boost }}
    - sqlite
    - zlib
    - zstd
  host:
    - boost {{ boost }}
    - boost-cpp {{ boost }}
    - sqlite
    - zlib
    - zstd
  run:
    - boost-cpp {{ boost }}

test:
  source_files:
    - example/example.15bits.bgen
    - example/example.16bits.bgen
    - example/example.16bits.bgen.bgi
  commands:
    - bgenix -help
    - bgenix -g example/example.16bits.bgen >/dev/null
    - bgenix -g example/example.15bits.bgen -index

about:
  home: https://enkre.net/cgi-bin/code/bgen
  license: BSL-1.0
  license_file: LICENSE_1_0.txt
  summary: Reference implementation of the BGEN format, written in C++

extra:
  recipe-maintainers:
    - matuskosut
